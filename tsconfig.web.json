{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "lib": [
            /* Specify library files to be included in the compilation. */
            "dom",
            "dom.iterable",
            "esnext"
        ]
    },
    "exclude": []
}
